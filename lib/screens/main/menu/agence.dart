import 'package:flutter/material.dart';

class GestionAgencesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Page de gestion des agences'),
      ),
    );
  }
}