import 'package:flutter/material.dart';

class GestionAgentsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gestion des agents'),
      ),
      body: Center(
        child: Text('Page de gestion des agents'),
      ),
    );
  }
}
